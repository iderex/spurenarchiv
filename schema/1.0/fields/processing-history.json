{
  "field": "processing_history",
  "quantity": "none",
  "requirement": "required",
  "publication": "published",
  "withoutThisField": "Nothing says whether the array is what the detector produced, so a smoothed trace and a raw one are read the same way and the width of the first is partly an artefact nobody can subtract.",
  "schema": {
    "type": "array",
    "items": {
      "type": "object",
      "required": [
        "step",
        "parameters"
      ],
      "additionalProperties": false,
      "properties": {
        "step": {
          "enum": [
            "background_subtraction",
            "normalisation",
            "interpolation",
            "resampling",
            "binning",
            "jitter_correction",
            "smoothing",
            "undescribed",
            "other"
          ]
        },
        "parameters": {
          "oneOf": [
            {
              "const": "unknown"
            },
            {
              "type": "object"
            }
          ]
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    }
  }
}
