{
  "field": "delay_sign_convention",
  "quantity": "none",
  "requirement": "required",
  "withoutThisField": "The sign of the retrieved chirp cannot be determined, because the two conventions in use are mirror images of one another and no inspection of the array distinguishes them.",
  "schema": {
    "enum": [
      "xuv_minus_dressing",
      "dressing_minus_xuv"
    ]
  }
}
