{
  "field": "target_composition",
  "quantity": "none",
  "requirement": "optional",
  "publication": "published",
  "withoutThisField": "A photoline from water or nitrogen in the gas line cannot be told from a feature of the target, so an unexplained band is available to be fitted as structure in the pulse.",
  "schema": {
    "type": "object",
    "required": [
      "components",
      "how_determined"
    ],
    "additionalProperties": false,
    "properties": {
      "components": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "required": [
            "species"
          ],
          "additionalProperties": false,
          "properties": {
            "species": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "fraction": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            }
          }
        }
      },
      "how_determined": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    }
  }
}
