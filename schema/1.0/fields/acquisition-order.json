{
  "field": "acquisition_order",
  "quantity": "none",
  "requirement": "optional",
  "publication": "published",
  "withoutThisField": "A slow drift cannot be told from a chirp, because a scan taken monotonically puts the drift onto the delay axis as a linear phase and nothing in the array distinguishes the two.",
  "schema": {
    "type": "object",
    "required": [
      "kind"
    ],
    "additionalProperties": false,
    "properties": {
      "kind": {
        "enum": [
          "monotonic_increasing",
          "monotonic_decreasing",
          "randomised",
          "interleaved",
          "other"
        ]
      },
      "index_sequence": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "integer",
          "minimum": 0
        }
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    }
  }
}
