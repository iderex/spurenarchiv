{
  "field": "dressing_carrier_envelope_phase",
  "quantity": "none",
  "requirement": "required",
  "withoutThisField": "A trace averaged over an unstabilised carrier-envelope phase and analysed as a single-phase trace yields a systematically distorted pulse, and nothing in the array says which of the two it is.",
  "schema": {
    "type": "object",
    "required": [
      "stabilised",
      "tagging"
    ],
    "additionalProperties": false,
    "properties": {
      "stabilised": {
        "type": "boolean"
      },
      "tagging": {
        "enum": [
          "shot_by_shot",
          "single_value",
          "averaged_over_distribution"
        ]
      },
      "value": {
        "type": "number"
      }
    }
  }
}
