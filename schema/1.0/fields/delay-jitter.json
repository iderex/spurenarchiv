{
  "field": "delay_jitter",
  "quantity": "time",
  "requirement": "optional",
  "publication": "published",
  "withoutThisField": "Shot-to-shot timing jitter cannot be separated from a genuinely longer pulse, which is the systematic that makes measured durations disagree between laboratories.",
  "schema": {
    "type": "object",
    "required": [
      "rms",
      "how_determined"
    ],
    "additionalProperties": false,
    "properties": {
      "rms": {
        "type": "number",
        "minimum": 0
      },
      "how_determined": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    }
  }
}
